# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.local

# Wrangler local cache (used by ad-hoc CLI invocations against R2)
.wrangler/

# Playwright e2e artifacts (failed-run videos/traces/screenshots,
# the HTML report, and the browser-binaries cache).
test-results/
playwright-report/
playwright/.cache/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test coverage output (vitest --coverage)
coverage/

# Staging e2e build env (regenerated from CI secrets / locally)
.env.e2e-staging
