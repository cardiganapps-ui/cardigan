<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Android App Links — auto-verified deep-link entry points for
                cardigan.mx. autoVerify="true" tells Play that this intent
                filter should be checked against the assetlinks.json file at
                https://cardigan.mx/.well-known/assetlinks.json. When the
                file's sha256_cert_fingerprints match the certificate that
                signed this APK, the OS opens cardigan.mx links directly
                in the app instead of routing them through Chrome.

                Until the release keystore is generated and its SHA-256
                lands in assetlinks.json, auto-verify will fail and links
                continue opening in the browser (no regression vs today).
                The intent filter still wins user-disambiguation prompts
                ("Open with Cardigan?"), so a user can manually opt in.

                Path patterns cover the entry points the cold-boot URL
                parser in src/App.jsx handles: patient invite, influencer
                code, app root (Stripe billing return arrives at /).
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="cardigan.mx" />
                <data android:pathPattern="/i/.*" />
                <data android:pathPattern="/c/.*" />
                <data android:pathPattern="/" />
            </intent-filter>

            <!--
                Home-screen long-press shortcuts (Android 7.1+). The
                shortcut entries live in res/xml/shortcuts.xml; each
                fires a deep-link intent at MainActivity with a
                query-string flag the embedded webview translates
                into an in-app action via App.jsx's shortcut receiver.

                Keep this list in sync with the PWA manifest's
                `shortcuts` array (public/manifest.json) so the same
                three actions surface on Capacitor APK installs and
                browser PWA installs.
            -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!--
            FCM notification defaults. Without an explicit small icon,
            Android falls back to the launcher icon rendered as an alpha
            mask — a white square. The channel id matches what the
            reminder cron sends (api/_fcm.ts channelId) and what
            nativePush.ts creates on permission grant.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_cardigan" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorPrimary" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="session_reminders" />
            <!-- CapacitorPasskey auto-configuration -->
        <meta-data
            android:name="asset_statements"
            android:resource="@string/capacitor_passkey_asset_statements" />
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        Strip the advertising-ID + Privacy Sandbox AdServices permissions
        that Google Play services (pulled in transitively by
        @capacitor/push-notifications / play-services-measurement)
        auto-adds at manifest-merge time. Cardigan shows no ads and does
        not read the advertising ID, so their presence contradicts the
        "No advertising ID" declaration in Play Console and blocks release
        validation (error: "release includes the AD_ID permission but your
        declaration says your app doesn't use advertising ID" — Play's gate
        is triggered by the whole AdServices family, not just AD_ID).
        Removing all of them keeps the manifest consistent with the
        declaration.
    -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_CUSTOM_AUDIENCE" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_TOPICS" tools:node="remove" />
</manifest>
